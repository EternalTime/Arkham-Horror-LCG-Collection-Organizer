{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the collection page (served from localhost) to call the save/load commands.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:17845", "http://localhost:17845"]
  },
  "permissions": ["core:default"]
}
